package com.example.zaribatodolist.presentation.fragmentHolder

import androidx.lifecycle.ViewModel
import com.example.zaribatodolist.data.repository.AuthRepository

class HolderViewModel(private val repository: AuthRepository) : ViewModel(){
}